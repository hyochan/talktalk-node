type User {
  id: ID! @unique
  email: String! @unique
  name: String
  photoUrl: String
  description: String
  createdAt: DateTime!
  updatedAt: DateTime!
  friends: [User!]!
}

type AuthPayload {
  token: String!
  user: User!
}
